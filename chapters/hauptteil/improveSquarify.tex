\chapter{Erweiterung der Treemap-Visualisierung} \label{sec:VerbesserungSquarify}
Da die Treemap-Visualisierung die weit verbreitetste Visualisierungsmethode für die Darstellung von Software-Qualitätsmetriken ist und in der Literaturrecherche (siehe Abschnitt \ref{sec:LiteraturRecherche}) zudem sehr gut abgeschnitten hat, macht es Sinn speziellen Augenmerk auf diese Visualisierung zu legen. Zudem wurde in der Auswertung der Literaturrecherche gefolgert, dass die Qualität der Treemap-Visualisierung stark schwanken kann und je nach Anwendungsfall optimiert werden sollte.
Natürlich gibt es auch andere Treempa-Algorithmen, wie zum Beispiel den Slice-and-Dice Algorithmus \cite{shneiderman1992tree} oder den Pivot-by-Middle Algorithmus \cite{shneiderman2001ordered}. Der Squarify Algorithmus zeigt aber in der Literatur die besten Ergebnisse in Bezug auf das Seitenverhältnis der Knoten \cite{shneiderman2001ordered}. Außerdem ist der Squarify Algorithmus \cite{bruls2000squarified}, wie er in Abschnitt \ref{sec:Squarify} beschrieben wurde, ist für das erstellen von Treemap-Layouts ein sehr vielversprechender Algorithmus, der von vielen Tools und Visualisierungen, die wir zuvor vorgestellt haben verwendet wird \cite{code_charta_github,sereene_website,codeCity1}. 
Wir werden diesen Algorithmus deshalb auf verschiedene Weisen erweitert und angepassen, um das Layout für unseren Anwendungsfall zu verbessern. Dabei wird das Layoutproblem, wie es in Abschnitt \ref{sec:TreemapProblem} beschrieben wurde, eine zentrale Rolle spielen, da dieses ein großes Problem für die Treemap-Visualisierung darstellt. Zudem werden aber am Anfang des Kapitels noch weitere Kriterien aufgestellt, die für die Qualität der Treemap-Visualisierung relevant sind und die in diesem Kapitel optimiert werden sollen.
Nach dem Aufstellen der Kriterien werden wir eine Datenanalyse von OpenSource Projekten durchführen, um mehr über typeische Strukturen von Software zu lernen. Zudem Dient die Datenanalyse dazu, ein Set an typischen, breit gefächerten und repräsentativen Software-Projekten zu auszuwählen, die für die Evaluation in diesem Kapitel verwendet werden.
Anschließend werden die verschiedenen Erweiterungen und Anpassungen des Squarify Algorithmus beschrieben und evaluiert, um am Ende einen Vorschlag für ein verbessertes Treemap-Layout zu machen, das für unseren Anwendungsfall besonders gut geeignet ist. 


\section{Kriterien für Treemap Layouts} \label{sec:CodeCityLayouts}
Um disese fünf Aspekte zu erreichen, definieren wir sechs Kriterien für das 2D layout, die diese Aspekte messbar machen. Diese Kriterien sollen helfen, die Qualität der Visualisierung zu bewerten und zu vergleichen. Die Kriterien sind:

\begin{itemize}
    \item \textbf{Knoten sichtbarkeit:} Es sollte keine Knoten geben, die aufgrund von Abständen oder anderen Gründen nicht sichtbar sind. Dieses Ziel spielt speziell auf das in abschnitt \ref{sec:TreemapProblem} beschriebene Problem ab. \enquote{Missing nodes are not addressed in the existing treemap literature. }\cite[5]{lu2008cascaded} Diesen Wert kann man einfach zählen, indem Alle Knoten gezählt werden, deren Länge oder Breite kleiner oder gleich 0 ist. Dieses Kriterium betrachten wir von allen Kriterien als das wichtigste, da für unseren Anwendungsfall die Sichtbarkeit aller Knoten essenziell ist. Dies ist vorallem dann der Fall, wenn das 2D Layout in 2.5D extrudiert wird. Selbst wenn ein Knoten eine sehr kleine Fläche hat, kann es sein, dass dieser Knoten eine sehr hohe Höhe oder eine schlechte Farbe hat, wodurch es trotzdem wichtig ist, diesen Knoten zu sehen.
    \item \textbf{Flächengröße:} Um die Korrelation mit dem Code zu gewährleisten, sollte die Fläche der Knoten proportional zum Metrikwert sein. Wir berechnen dies einfach indem wir die Fläche des Knotens durch den Metrikwert teilen. Im Optimalfall sollte dieser Wert für alle Knoten gleich sein, um eine perfekte Proportionalität zu gewährleisten. Wir messen also die Abweichung des Wert-Fläche-Verhältnisses vom Mittelwert. Um diese Werte besser einordnen und vergleichen zu können, werden wir den Varianzkoeffizienten verwenden. Dies betrachten wir als das zweitwichtigste Kriterium, da die Fläche der Knoten eine wichtige Visuelleeigenschaft ist, die möglichst genau den Metrikwerten entsprechen sollte. 
    \item \textbf{Seitenverhältnis:} Um die visuelle Komplexität zu reduzieren und die Verständlichkeit zu erhöhen, sollte das Seitenverhältnis der Knoten möglichst nahe bei 1:1 liegen. Dies verbessert die Lesbarkeit der Knoten und macht es einfacher, die Informationen zu erfassen. Wir teilen einfach die größere Seitenlänge durch die kleinere Seitenlänge, um das Seitenverhältnis zu berechnen. Ein Wert von 1 bedeutet also ein perfektes Quadrat, während höhere Werte auf längliche Rechtecke hinweisen.
    \item \textbf{Platznutzung:} Es sollte so wenig Fläche wir möglich ohne Informationsgehalt bleiben. Als gegenbeispiel kann man die Order-Knoten sehen, wie sie in der CodeCity Arbeit beschrieben wurden, bei denen die Fläche der Knoten nicht proportional zur Anzahl der Zeilen im Code ist, wodurch die Fläche an sich keinen Informationsgehalt mehr hat und außerdem viel leere Fläche entsteht. Wir messen dase, indem wir die Fläche aller Blatt-Knoten durch die Fläche des Wurzelknotens teilen. Ein Wert von 1 bedeutet also, dass die gesamte Fläche des Wurzelknotens von den Blatt-Knoten eingenommen wird. 
    \item \textbf{Zeitaufwand:} Die Generierung des Layouts sollte in einem angemessenen Zeitrahmen erfolgen, um eine schnelle Visualisierung zu ermöglichen. Dies verbessert die Skalierbarkeit und generelle Nutzbarkeit der Visualisierung. Anzumerken ist, dass alle berechnungen auf einem M2 MacBook durchgeführt wurden, und alles in typescript implementiert wurde. Das ist auch praxisnah, da viele Tools in JavaScript/TypeScript implementiert sind und bei Nutzern lokal gerendert werden \cite{code_charta_github,sereene_website,codeIsBeautiful}. Anzumerken ist, dass die gemessene Zeit nur die Ausführung des Layout-Algorithmus beinhaltet und nicht die Zeit für das Rendering der Visualisierung.
\end{itemize}


\enquote{Especially for the node weights, we assess if the algorithms use the weight values to scale the sizes of leaf nodes. Typically, the size of parent nodes is adjusted to the spatial consumption of the child nodes.}\cite[3]{scheibel2020survey} - Scheibel et al. sagen also, für sie ist es nicht so wichtig, dass die Größe der Elternknoten proportional zu den Metrikwerten der Knoten ist.

\input{chapters/hauptteil/datenAnalyse}

\section{Abstände zwischen Knoten} \label{sec:AbständeZwischenKnoten}
Wie zuvor beschrieben, entsteht das Treemap-Problem erst durch das Hinzufügen von Abständen zwischen den Knoten (siehe Abschnitt \ref{sec:TreemapProblem}). Je Größer die Abstände sind, desto größer wird das Problem. 

Dabei ist die Wahl des Abstands erstmal subjektiv und kommt auf die Software an und darauf, was man darstellen will. Will man den Fokus auf Struktur oder auf Metriken legen? 

\begin{quote}
    Nesting offsets give users control over the allocation of display space between internal and leaf nodes. Larger offsets put greater emphasis on internal nodes and hence the structure of the hierarchy; smaller offsets emphasize leaf nodes. \cite[48]{johnson1993treemapsPhd}
\end{quote}


Alle Messwerte und Ergebnisse der Evaluation hängen dabei natürlich sowohl von den Analysierten Daten als auch von der Wahl der Größe des Abstands ab. Je nachdem wie groß der Abstand gewählt wird, desto größer wird das Treemap Problem, man will aber tendenziell nicht zu kleine Abstände, damit man besser die Struktrur der Software erkennen kann. Aus diesem Grund werden wir in diesem Abschnitt die Kriterien immer in Abhängigkeit der Abständsgrößen betrachten. Wichtig zu erwähnen ist, dass die Eingabe des Abstands absolut ist. Für die Visualisierung am Ende zählt aber natürlich der Relative abstand im verhältnis zu der Größe der Abbildung. Wenn es Absoluter abstand von 10 Einheiten zwischen zwei Dateien ist, dann wirkt dieser bei einer Abbildung von 1000x1000 Einheiten natürlich deutlich kleiner als bei einer Abbildung von 200x200 Einheiten. Aus diesem Grund geben wir den Abstand immer Prozentual zur Größe an. Ein Wert von zum Beispiel einem Prozent bedeutet, dass der Abstand zwischen zwei Knoten 1\% der Seitenlänge des Wurzelknotens beträgt. Dadurch, dass durch das Hinzufügen von Abständen, auch die Fläche des Wurzelknotens größer wird ist dieser Zusammenhang nicht linear, wie in Diagram \ref{fig:basic_aimedRelativeMargin_over_marginInput} zu sehen ist. 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basic/aimedRelativeMargin_over_marginInput.png}
    \caption[Absoluter Abstand zu Relativer Abstand]{Zusammenhang zwischen der Absoluten Abstandseingabe und dem Relativen Abstand im Verhältnis zur Seitenlänge des Wurzelknotens}
    \label{fig:basic_aimedRelativeMargin_over_marginInput}
\end{figure}

In Abbildung \ref{fig:basic_react_200_to_4} und \ref{fig:basic_qwen3_4_to_4} zeigt, dass bei verschiedenen Softwares verschiedene Eingabe Abstände genutzt werden müssen, um den gleichen relativen Abstand zu erreichen. 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basic/react_200_to_4.png}
    \caption[React mit relativem Abstand von 0,4]{Layout mit einem Absoluten Abstand von 200 und einem Relativen Abstand von 0,4 (generiert aus dem React Repository \cite{githubreact_2025})}
    \label{fig:basic_react_200_to_4}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basic/qwen3_4_to_4.png}
    \caption[Qwen3 mit relativem Abstand von 4]{Layout mit einem Absoluten Abstand von 4 und einem Relativen Abstand von 4 (generiert aus dem Qwen3 Repository \cite{githubqwen3_2025})}
    \label{fig:basic_qwen3_4_to_4}
\end{figure}

Je nach Software Größe, Struktur und Ziel der Visualisierung kann es also sinnvoll sein, verschiedene Abstände zu wählen. Bei kleinen Softwareprojekten zum Beispiel kann es sinnvoll sein, auch größere relative Abstände zu wählen, um die Struktur der Software besser erkennen zu können, weil man auch bei größeren Abständen die einzelnen Dateien und deren WErte gut erkennen kann (siehe Abbildung \ref{fig:basic_qwen3_10_to_10}).

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basic/qwen3_10_to_10.png}
    \caption[Qwen3 mit relativem Abstand von 1]{Layout mit einem Absoluten Abstand von 10 und einem Relativen Abstand von 1 (generiert aus dem Qwen3 Repository \cite{githubqwen3_2025})}
    \label{fig:basic_qwen3_10_to_10}
\end{figure}

In dieser Arbeit werden wir daher die Kriterien immer in Abhängigkeit des Relativen Abstands betrachten, dabei werden wir Werte bis ca. 3\% betrachten, da darüber hinaus die Abstände unnütig groß werden (siehe Abbildung \ref{fig:basic_qwen3_100_to_35}).

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basic/qwen3_100_to_35.png}
    \caption[Qwen3 mit relativem Abstand von 35]{Layout mit einem Absoluten Abstand von 100 und einem Relativen Abstand von 35 (generiert aus dem Qwen3 Repository \cite{githubqwen3_2025})}
    \label{fig:basic_qwen3_100_to_35}
\end{figure}

Alles in allem Empfehlen wir also einen Abstand von 0.5\% bis 3\%, je nach Software und Ziel der Visualisierung.


\section{Angestrebtes Seitenverhältnis} \label{sec:Seitenverhältnis}
Wie bereits in den grundlagen erwähnt (siehe Abschnit \ref{sec:Squarify}) muss der angestrebte Seitenverhältniswert nicht zwingend 1 sein. Die bekannte d3.js implementierung des Squarify Algorithmus \cite{d3_treemap_code} nutzt zum Beispiel den Goldenenschnitt (1.618) als Zielwert für das Seitenverhältnis. Dieser Zielwert wird als Faktor in die bedingung des Algorithmus eingebaut, der bestimmt, wann eine Reihe komplett ist, dabei ist dann das optimale Seitenverhältnis nicht 1, sondern eben der angestrebte Wert. Will wollen uns hier nicht streiten, ob 1 oder der Goldene Schnitt am ende schönerer und übersichtlichere Layouts erzeugt, sondern wir zweifeln an, dass diese Zielwerte überhaupt sinnvoll sind, da man wie am Ende von Abschnitt \ref{sec:Squarify} an den Beiden beispielen sieht, die Ziel ratios nicht erreicht werden können (Ziel Seitenverhältnis von 1 führt zu einem Durchschnittlichen Seitenverhältnis von 1,42 und Ziel Seitenverhältnis von 5 führt zu einem Durchschnittlichen Seitenverhältnis von 2,79 - siehe Abbildung \ref{fig:squarifyRatio1} und \ref{fig:squarifyRatio5})
speziell wenn wir uns hierarchien Anschauen, die tief verschachtelt sind und pro Ordner nur wenige Kindknoten haben (siehe Datenanalyse Abschnit \ref{sec:strukturVonSoftwareprojekten}), vermuten wir, dass diese Ziele unnereichbar sind, da die Struktur der Daten das Layout zu stark beeinflusst. 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/aimedRatioZuRatio.png}
    \caption{Zusammenhang zwischen dem angestrebten Seitenverhältnis und dem tatsächlich erreichten Seitenverhältnis bei der Anwendung des Standard d3.js Squarify Algorithmus auf die ausgewählten Repositories.}
    \label{fig:aimedRatioZuRatio}
\end{figure}

In Abbildung \ref{fig:aimedRatioZuRatio} ist der Zusammenhang zwischen dem angestrebten Seitenverhältnis und dem tatsächlich erreichten Seitenverhältnis abbgebildet. Es ist klar zu erkenne, dass nie der angestrebte Wert erreicht wird, das tatsächliche Seitenverhältnis ist immer deutlich höher. Die Werte um 1 herum liefern dabei noch die besten Ergebnisse, trotzdem kommt hier kein Zielwert überhuapt in die nähe von dem Goldenenschnitt geschweige denn 1. ein zielwert vom goldenen Schnitt liefert ein median Seitenverhältnis von 7.35	mit durchschnitt 18.80, was deutlich schlechter ist als ein zielwert von 1 mit median 6.58 und durchschnitt 16.44.
Interessant ist es, wenn man sich die WErte um 1 herum in tabelle \ref{tab:seitenverhaeltnis_um_1} anschaut.

\begin{table}[h]
    \centering
    \begin{tabular}{|c|c|c|}
        \hline
        \makecell{\textbf{Angestrebtes}\\\textbf{Seitenverhältnis}} & \makecell{\textbf{Median des}\\\textbf{Seitenverhältnisses}} & \makecell{\textbf{Durchschnittliches}\\\textbf{Seitenverhältnis}} \\
        \hline
        0.900 &	6.82	& 16.32 \\
        0.990 &	6.57	& 16.44 \\
        1.000 &	6.58	& 16.44 \\
        1.025 &	6.37	& 16.55 \\
        1.050 &	6.28	& 16.17 \\
        1.100 &	6.30	& 16.17 \\
        1.150 &	6.32	& 16.67 \\
        1.200 &	6.40	& 16.78 \\
        1.300 &	6.79	& 16.89 \\
        \hline
    \end{tabular}
    \caption{Durchschnittliches und median Seitenverhältnis bei verschiedenen angestrebten Seitenverhältnissen um 1 herum bei der Anwendung des Standard Squarify Algorithmus auf die ausgewählten Repositories.}
    \label{tab:seitenverhaeltnis_um_1}
\end{table}

Anders als erwartet liefert ein Angestrebtes Seitenverhältnis von 1 nicht das beste Ergebnis, sondern ein Wert von 1.05 liefert das beste Ergebnis. Es ist zwar im Median nur um ca. 5\% besser, aber immerhin.  Warum dass der Fall ist, ist schwer nachzuvollziehen, vielleicht kann es in manchen Fällen besser sein leicht schlechtere Seitenverhältnisse of höheren Ebenen zu haben, weil dass dann zu besseren Seitenverhältnissen auf niedrigeren Ebenen führt, aber das ist nur eine Vermutung. Wir werden in dieser Arbeit den Wert von 1.05 als angestrebtes Seitenverhältnis verwenden, da dieser Wert die besten Ergebnisse für unserer Softwaremetrik daten liefert.


\section{Zweifache Berechnung} \label{sec:ZweifachBerechnung}
Die Grundlegende Idee dieser Erweiterung ist es, dass sich die Fläche der Knoten mit Abstand durch das Layout und die Fläche der Knoten ohne Abstand approximieren lässt. Die Idee ist es also einen ersten Durchlauf zu machen, bei dem das Layout ohne Abstand berechnet wird. Dann werden die Größen der Knoten entsprechend dem Layout angepasst, sodass die Größe der Knoten nun auch den Abstand berücksichtigt. Anschließend wird ein zweiter Layout-Schritt mit diesen angepassten Größen durchgeführt, um das finale Layout zu berechnen.

Bevor wir uns die Details und Ergebnisse dieser Erweiterung anschauen, wollen wir vorweg nehmen, dass diese Erweitung natürlich nicht optimal funktionieren kann und das auch klar ist, da sich die die Änderung der Größe der Knoten natürlich auch das Layout im Zeweiten Durchlauf ändern wird, wodurch die Größen der Knoten wieder nicht korrekt sind. Was ja überhaupt erst das Grundlegende Problem ist (siehe Abshnictt \ref{sec:Problemstellung}). Allerdings ist es ein erster Schritt sich dem Problem zu nähern und zu schauen, ob es sich lohnt in diese Richtung weiter zu forschen.

Der Grundlegende Algorithmus bleibt also (fast) gleich, nur dass zwischen dem ersten und dem zweiten Durchlauf ein zusätzlicher Schritt \textit{Größenanpassung} eingefügt wird. Die Einzige änderung die vorgenommen werden muss ist, dass Knoten nur mit dem definierten Abstand zwischen dem Elternknoten platziert werden können und generell die Fläche des Elternknotens um den Abstand verkleinert wird. Wir bezeichnen den Schritt bei dem der Squarify Algorithmus durchgeführt wird als \textit{Layout-Schritt} und den Schritt bei dem die Größen der Knoten angepasst werden als \textit{Größenanpassung}.
Außerdem ist es nötig nach dem zweiten Durchlauf die Knoten, deren größenwert ja nun den abstand beinhalten, zu verkleinern, um auch den abstand zwischen den Geschwistern herzustellen. Es ist zu erkennen, dass dadurch der Abstand sowohl zwischen Geschwistern als auch zu den Elternknoten den doppelten wert des definierten Abstands hat, dieses Problem ignorieren wir hier, da man es trivialierweise lösen könnte, indem man immer nur die hälfte des Abstands zwischen Geschwistern und Elternknoten abzieht, was wir hier der einfachheit halber nicht tun. -- ODER VIELLEICHT HIER IN DER THESIS DOCH? DANN KÖNNTE ICH MIR DIESEN ABSCHNITT SPAREN, AUCH WENN ES IN DER IMPLEMENTIERUNG AM ENDE ANDERS IST

Wir stellen verschiedene Ansätze vor, was sowohl die Größenanpassung als auch die Anpassung der Knoten nach dem zweiten Durchlauf angeht.
Die Algorithmen funktionieren allerdings alle nach ähnlichem Prinzip: Es wird zunächst für jeden Knoten die Fläche die der Abstand in diesem Layout benötigen würde addiert, indem die Fläche aus der neuen Länge (alte Länge + 2 mal den Abstand) und der neuen Breite (alte Breite + 2 mal den Abstand) berechnet wird. Zusätzlich wird für Elternknoten die Flächenvergrößerung aller Kindernkoten addiert. An dieser Stelle ist allerdings nicht klar, wie sich die Flächenvergrößerung der Kinderknoten auf die Fläche der Elternknoten auswirkt, da diese Änderung selbst von der Anordnung der Kinderknoten abhängt. Wir testen verschiedene Ansätze, um die Flächenänderung der Elternknoten in Abhängikeit zu der Flächenänderung der Kinderknoten zu approximieren.

Nach dem zweiten Durchlauf wird nun die Fläche der Knoten so reduziert, dass sowohl der Abstand zwischen Geschwistern als auch der Abstand zu den Elternknoten den gewünschten Wert hat. Dies ist straight forward und wird hier nicht weiter erläutert. Anzumerken ist aber das dieser Schritt speziell abhängt von der Art der Größenanpassung, die im ersten Schritt durchgeführt wurde.



\subsection{Größenanpassung}
Es gibt verschiedene Ansätze, wie die Größenanpassung durchgeführt werden kann. Grundsätzliches Ziel der Größenanpassung ist es, die Fläche der Knoten so zu vergrößern, dass diese Fläche den Abstand zwischen den Knoten möglichst gut approximiert. Wir testen hier zwei Ideen aus.

Der erste ansatz ist ähnlich zu dem cascaded ansatz. Die Fläche der Knoten wird um den Abstand in beiden Richtungen vergrößert. Zusätzlich wird die Fläche der Elternknoten um die Fläche der Kinderknoten vergrößert. wir zeigen genau auf, warum dieser Ansatz große Schwächen hat. bzw Pseudocode für den Algorithmus \ref{alg:EinfacheGrößenanpassung}.


\begin{algorithm}[H]
    \begin{algorithmic}[1]
        \Function{absoluteValueIncrease}{node: SquarifyNode, margin: number}
        \State childrenValueIncrease $\gets$ 0
        \If{node.children}
        \For{child in node.children}
        \State childrenValueIncrease += absoluteValueIncrease(child, margin)
        \EndFor
        \EndIf
        \State valueIncrease $\gets$ width * margin * 2 + length * margin * 2 + margin * margin * 4 + childrenValueIncrease
        \State node.value += valueIncrease
        \State \Return valueIncrease
        \EndFunction
    \end{algorithmic}
    \caption[Absolute Größenanpassung]{Absolute Größenanpassung}
    \label{alg:EinfacheGrößenanpassung}
\end{algorithm}

Das Problem des Algorithmuses ist am besten an einem Beispiel zu verdeutlichen. Wir visualieren den ZWITEN AnHANG - auch wieder eine händisch erstellte Map, um das Problem zu verdeutlichen. In Abbildung \ref{fig:zeroMarginSquarifyArtifialTwo} ist das Layout mit einem Abstand von 0 zu sehen. 

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/zeroMarginSquarifyArtifialTwo.png}
    \caption{Treemap Layout generiert mit dem Squarify Algorithmus nach Abschnitt \ref{sec:Squarify} mit einem Abstand von 0 und der einfachen Größenanpassung auf der händisch erstellen map (siehe Anhang).}
    \label{fig:zeroMarginSquarifyArtifialTwo}
\end{figure}

In Abbildung \ref{fig:simpleIncreaseMarginOne} ist das Layout mit einem Abstand von 1 zu sehen. Es ist zu erkennen, dass die Knoten auf der linken Seite des Layouts sich außerhalb ihrer Elternknoten erstrecken. Warum passiert das? Knoten 10 ist im zweiten Layout-Schritt deutlich schmaler als im ersten Layout-Schritt. Dadurch wird die Fläche die durch den Abstand eingenommen wird größer als angenommen, weshalb die Fläche des Knotens nach abzug des Abstands im letzen Schritt kleiner ist, als gewünscht. Dementsprechend ist auch die Fläche der Knoten unten links größer als gewünscht, da das Layout der Knoten im zweiten Layout-Schritt quadratischer wird. Knoten 5, der Knoten, der am Quadratischsten ist, wird also am größten erscheinen. Obwohl beide den selben wert haben ist Knoten 5 ca. 1,2 mal größer als Knoten 10) In diesem Beispiel erscheint der unterschied kaum merklich, aber es gibt ihn trotzdem und in anderen fällen kann dieser Unterschied merklich werden.
Viel signifikanter ist aber der Effekt, dass Elternknoten ebenfalls immer schmaler werden, wodurch die fläche, die der innerere abstand einnimm, ebenfalls größer wird und dass sogar immer mehr von ebene zu ebene, wenn man runter geht. Dadurch wird die Fläche für die Kindknoten immer kleiner, was dazu führt, dass Knoten teilweise über ihre Elternknoten hinauswachsen.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/simpleIncreaseMarginOne.png}
    \caption{Treemap Layout generiert mit dem Squarify Algorithmus nach Abschnitt \ref{sec:Squarify} mit einem Abstand von 1 und der absoluten Größenanpassung auf der händisch erstellen map (siehe Anhang).}
    \label{fig:simpleIncreaseMarginOne}
\end{figure}

Dieser Effekt kann einfach behoben werden, wie wir in Abschnitt \ref{sec:ScalingKnoten} sehen werden.

Bei der berechnung zuvor wurde einfach der margin fläche eines Elternknotens berechnet, auf basis der alten Fläche, ohne die Flächenädnerung durch die Kinderknoten zu berücksichtigen. dadurch wird die resultierende Fläche der Elternknoten zu klein sein, da die Fläche der Kinderknoten nicht berücksichtigt wird.
Wir stellen folgend eine zweite Version der Größenanpassung vor, die versucht die Fläche der Elternknoten durch die Fläche der Kinderknoten zu approximieren. Wir werden dann beide Versionen vergleichen und schauen, welche besser funktioniert.
Die Idee bei dieser Erweiterung ist es die Änderung der Kinder schon vor dem Hinzufügen der Abstandsfläche zu berücksichtigen. Dafür muss die relative Flächenänderung durch die Kindknoten berechnet werden. und damit dann die Seitenlängen anpassen und dann die margins hinzufügen, um die neue Fläche zu erhalten.
Der Algorthmus wird im foldenden als Pseudocode dargestellt (siehe Algorithmus \ref{alg:RelativeGrößenanpassung}). 


\begin{algorithm}[H]
    \begin{algorithmic}[1]
        \Function{relativeValueIncrease}{node: SquarifyNode, margin: number}
        \State width $\gets$ node.x1 - node.x0
        \State length $\gets$ node.y1 - node.y0
        
        \If{node.isLeaf}
        \State valueIncrease $\gets$ width * margin + length * margin + margin * margin * 2
        \State node.value += valueIncrease
        \State \Return valueIncrease
        \EndIf
        
        \State childrenValueIncrease $\gets$ 0
        \If{node.children}
        \For{child in node.children}
        \State childrenValueIncrease += relativeValueIncrease(child, margin)
        \EndFor
        \EndIf
        
        \State ratioChildrenValueIncrease $\gets$ (node.value + childrenValueIncrease) / node.value
        
        \State valueIncrease $\gets$ 
        Math.sqrt(ratioChildrenValueIncrease) * width * margin * 2 +
        Math.sqrt(ratioChildrenValueIncrease) * length * margin * 2 +
        margin * margin * 4 +
        childrenValueIncrease
        
        \State node.value += valueIncrease
        \State \Return valueIncrease
        \EndFunction
    \end{algorithmic}
    \caption[Relative Größenanpassung]{Relative Größenanpassung}
    \label{alg:RelativeGrößenanpassung}
\end{algorithm}

Problem:
Der Algorithmus in dieser Form zeigt einige Probleme auf. Die Flächenvergrößerung der Kindknoten sagt nichts darüber aus, in welche Richtung sich die Fläche ändert. Es wird davon ausgegangen, dass sich die Fläche gleichmäßig in beide Richtungen ändert (siehe Zeile 13 und 14 in Algorithmus ZEILEN ANPASSEN  \ref{alg:RelativeGrößenanpassung}). 
es kommt also zu ähnlichen Problem wie davor. es können knoten sowohl zu viel platz als auch zu wenig Platz bekommen, jenachdem ob Sie im zweiten schritt quadratischer oder schmaler werden.  Siehe Abbildung \ref{fig:relativeIncreaseMarginOne} für ein Beispiel.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/increaseMarginOne.png}
    \caption{Treemap Layout generiert mit dem Squarify Algorithmus nach Abschnitt \ref{sec:Squarify} mit einem Abstand von 1 und der relativen Größenanpassung auf der händisch erstellen map (siehe Anhang).}
    \label{fig:relativeIncreaseMarginOne}
\end{figure}


\subsection*{Scaling der Knoten} \label{sec:ScalingKnoten}
Beide Ansätze der Größenanpassung haben das Problem, dass die Fläche der Knoten nicht exakt die Fläche ist, die der Abstand benötigt. Dadurch können Probleme entstehen.

Wenn die Fläche innnerhalb von Elternknoten immer kleiner wird, kann es passieren, dass Knoten über ihre Elternknoten hinauswachsen, wie in Abbildung \ref{fig:simpleIncreaseMarginOne} zu sehen ist. Es kann genauso passieren, dass die Fläche innerhalb der Knoten größer wird, wie in Abbildung \ref{fig:relativeIncreaseMarginOne} auf der linken seite zu erkennen ist. Dieser Effekt kann trivialer weise behoben werden, indem der zweite Layout-Schritt angepasst wird, sodass die Knoten immer auf die Fläche des Elternknotens skaliert werden. 
Vor jeden Squarify-Schritt wird dafür die wirklich zur verfügung stehende Fläche des Elternknotens berechnet und die Kindknoten entsprechend dieser Änderung skaliert, sodass sie genau in die Fläche des Elternknotens passen.

Der Nachteil dieser Methode ist in Abbildung \ref{fig:simpleIncreaseMarginOneScale} zu erkennen. Die Knoten werden dadurch natürlich nicht mehr proportional zu ihren Werten sein. Knoten 10 hat zum beispiel ein Verhältnis von ca. 0.5 zu seinem Wert, während Knoten 6 ein Verhältnis von ca. 1.4 zu seinem Wert hat.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/simpleIncreaseMarginOneScale.png}
    \caption{Treemap Layout generiert mit dem Squarify Algorithmus nach Abschnitt \ref{sec:Squarify} mit einem Abstand von 1 und der einfachen Größenanpassung auf der händisch erstellen map (siehe Anhang) und der Skalierung der Knoten.}
    \label{fig:simpleIncreaseMarginOneScale}
\end{figure}

Je genauer die Größenanpassung der Knoten ist, desto geringer fällt natürlich dieser Effekt aus. Siehe im Vergleich dazu Abbildung \ref{fig:relativeIncreaseMarginOneScale}, da die relative Größenanpassung deutlich genauer ist, ist der Effekt hier auch deutlich geringer. Knoten 10 hat Größe 40 und Knoten 6 hat Größe 41, was fast ähnlich Groß ist.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/increaseMarginOneScale.png}
    \caption{Treemap Layout generiert mit dem Squarify Algorithmus nach Abschnitt \ref{sec:Squarify} mit einem Abstand von 1 und der relativen Größenanpassung auf der händisch erstellen map (siehe Anhang) und der Skalierung der Knoten.}
    \label{fig:relativeIncreaseMarginOneScale}
\end{figure}

Persönlich würde ich sagen, dass dieser Effekt für das herunter skalieren der Knoten gut ist, da sonst eine grundlegende eigenschaft der Darstellung verletzt wird, aber für das hoch skalieren der Knoten könnte man auch sagen, dass es wichtiger ist die Fläche der Knoten proportional zu ihren Werten zu halten, als die \textit{verschwendete} Fläche aufzufüllen. Wir entscheidung uns in dieser Arbeit dafür, dass wir die Knoten immer skalieren, auch nach oben, um die Identität der Darstellung von Treemap-Layouts zu wahren. Dadurch tauschen wir sozusagen Proportionalität der Knoten gegen Platznutzung ein.

\subsection*{Vergleich der Ansätze} \label{sec:VergleichDerAnsätze}

Wir vergleichen diese beiden Ansätze jetzt mit dem STandard Squarify nested squarify algorithmus von d3.js, wie er von Mike Bostock implementiert wurde \cite{d3_nested_2023}.


\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basicCompare/numberOfZeroAreaNodes_over_aimedRelativeMargin.png}
    \caption[Fehlende Knoten über relativen Abstand]{Anzahl der fehlenden Knoten in Abhängigkeit des relativen Abstands für die drei verschiedenen Größenanpassungen.}
    \label{fig:basicCompare_numberOfZeroAreaNodes_over_aimedRelativeMargin}
\end{figure}

In Abbildung \ref{fig:basicCompare_numberOfZeroAreaNodes_over_aimedRelativeMargin} klar zu erkennen, dass das beide Erweiterungen des Squarify Algorithmus deutlich weniger fehlende Knoten haben als der Standard Squarify Algorithmus von d3.js. Eine wirklich extreme Verbesserung ist zu erkennen, die speziell bei größeren Abständen noch stärker wird. Während der Standard Squarify Algorithmus bei einem Abstand von 1\% im Schnitt ca. 600 fehlende Knoten hat, haben die beiden Erweiterungen fast keine fehlenden Knoten mehr. 
Zudem ist zu erkennen, dass die absolute Größenanpassung bis 2,5\% etwas mehr fehlende Knoten hat als die relative Größenanpassung.
Außerdem ist zu erkennen, dass die relative Größenanpassung auch noch bei größeren Abständen weniger fehlende Knoten hat und konstanter bleibt, während die absolute Größenanpassung bei Abständen über 2,5\% höhere Ergebnisse erzielt.

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basicCompare/variationCoefficientOfAreaDifferences_over_aimedRelativeMargin.png}
    \caption[Flächenverhältnis über relativen Abstand]{Varianzkoeffizient des Flächenverhältnisses in Abhängigkeit des relativen Abstands für die drei verschiedenen Größenanpassungen.}
    \label{fig:basicCompare_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin}
\end{figure}

Bei allen drei Algorithmen ist zu erkennen, dass der Varianzkoeffizient des Flächenverhältnisses mit zunehmendem Abstand steigt (siehe Abbildung \ref{fig:basicCompare_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin}). Das bedeutet, dass die Fläche der Knoten mit zunehmendem Abstand immer unproportionaler zu ihren Werten wird.
Klar zu erkennen ist, dass das schlechteste Seitenverhältnis durch den d3.js squarify Algorithmus erzielt wird. Zweit bestes durch die relative Größenanpassung und das beste durch die absolute Größenanpassung (siehe Abbildung \ref{fig:basicCompare_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin}). 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basicCompare/averageRatio_over_aimedRelativeMargin.png}
    \caption[Seitenverhältnis über relativen Abstand]{Durchschnittliches Seitenverhältnis in Abhängigkeit des relativen Abstands für die drei verschiedenen Größenanpassungen.}
    \label{fig:basicCompare_averageRatio_over_aimedRelativeMargin}
\end{figure}


In Abbildung \ref{fig:basicCompare_averageRatio_over_aimedRelativeMargin} ist zu erkennen, dass der d3.js ab ca. 0,5\% Abstand Seitenverhältnisse von 4,5 bis 5 erzielt, während die beiden Erweiterungen des Squarify 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basicCompare/spaceForLeafNodesRatio_over_aimedRelativeMargin.png}
    \caption[Platznutzung über relativen Abstand]{Platznutzung in Abhängigkeit des relativen Abstands für die drei verschiedenen Größenanpassungen. Werte näher bei 1 sind besser.}
    \label{fig:basicCompare_spaceForLeafNodesRatio_over_aimedRelativeMargin}
\end{figure}

In Abbildung \ref{fig:basicCompare_spaceForLeafNodesRatio_over_aimedRelativeMargin} ist zu erkennen, dass die Platznutzung bei den beiden Erweiterungen dauerhaft näher an der 1 ist. Theoretisch hätten wir erwartet, dass alle drei Varianten ähnliche Werte produzieren, da der relative Abstand ja bei allen gleich ist. Wir erklären uns das dadruch, dass ja auch mehr Knoten fehlen, wodurch dann die Fläche der Knoten auch kleiner wird.

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/basicCompare/timeNeededInMs_over_aimedRelativeMargin.png}
    \caption[Zeitaufwand über relativen Abstand]{Zeitaufwand in Abhängigkeit des relativen Abstands für die drei verschiedenen Größenanpassungen.}
    \label{fig:basicCompare_timeNeededInMs_over_aimedRelativeMargin}
\end{figure}

In Abbildung \ref{fig:basicCompare_timeNeededInMs_over_aimedRelativeMargin} ist zu erkennen, dass der Zeitaufwand bei den beiden Erweiterungen dauerhaft höher ist. Teilweise doppelt so hoch, teilweise weniger. Das ist natürlich zu erwarten, da ja ein zweiter Layout-Schritt durchgeführt wird. Die relative Größenanpassung ist dabei etwas aufwändiger als die absolute Größenanpassung, da hier ja auch die relative Flächenänderung der Kindknoten berechnet werden muss. Allerdings ist der Unterschied hier nicht so groß. Anzumerken ist, dass das verschwinden von Knoten keinen Einfluss auf die Laufzeit des d3.js Squarify Algorithmus hat, da dieser zuerst das Layout berechnet und dann die Abstände abzieht, bei den angepassten Versionen ist. In den angepassten Versionen implementieren wir early stopping, sobald die Fläche eines Knotens 0 oder negativ wird, was aber nicht so oft passiert, dass es einen großen Einfluss auf die Laufzeit hat.

Alles in allem schneiden die beiden Erweiterungen des Squarify Algorithmus deutlich besser ab als der Standard Squarify Algorithmus von d3.js. Die absolute Größenanpassung schneidet dabei beim Wert-Flächen-Verhältnis besser ab, während die relative Größenanpassung bei der Anzahl der fehlenden Knoten besser abschneidet. Beide Erweiterungen sind valide und zu Begründende alternativen zum Standard Squarify Algorithmus von d3.js. Für die weitere Arbeit in dieser Thesis entscheiden wir uns für die relative Größenanpassung, da wir hier die Anzahl der fehlenden Knoten als das wichtigste Kriterium ansehen.









\section{Reihenfolge der Knoten im ersten Layoutschritt} \label{sec:ReihenfolgeKnoten}
Die Reienfolge in der die knoten in das Layout eingefügt werden, hat einen großen Einfluss auf das Layout. \cite{johnson1991tree} haben in ihrem Paper herausgefunden, dass die ergebnisse am besten sind, wenn die Knoten in der Reihenfolge der Größe eingefügt werden. Also der Größte zuerst. Sie begründen das damit, dass sich dadruch bessere Seitenverhältnisse ergeben.
Außerdem haben wir in Abschnitt \ref{sec:strukturVonSoftwareprojekten} gezeigt, dass 95\% der Ordner maximal 18 Kindknoten haben. Das sortieren aller Knoten in einem Ordner nach Größe ist also gar nicht mal so Zeitaufwendig, wie man denken könnte, wenn man tausende von knoten hat, weil immer nur ein kleines set von knoten sortiert werden muss. Kleines Beispiel:
Wenn wir von einem typischen sortier algorithmus mit einer Laufzeit von $\mathcal{O}(n\log n)$ (zB. quicksort), einer anzahl von 4221 Knoten (Durchschnittwert der Anzahl der Knoten siehe Abschnitt \ref{sec:strukturVonSoftwareprojekten}) und 18 Knoten pro Ordner. 
Wenn wir alle Knoten auf einmal sortieren wollen: 

\[
T_1 = n \cdot \log_2(n) = 4221 \cdot \log_2(4221)
\]

\[
\log_2(4221) \approx 12.04
\]

\[
T_1 \approx 4221 \cdot 12.04 \approx 50821
\]

Wenn wir die Knoten in 18er bundleln sortieren wollen: 

\[
T_2 = m \cdot \left(k \cdot \log_2(k)\right) = 235 \cdot \left(18 \cdot \log_2(18)\right)
\]

\[
\log_2(18) \approx 4.17
\]

\[
T_2 \approx 235 \cdot (18 \cdot 4.17) = 235 \cdot 75.06 \approx 17639
\]

D3.js hat das nicht im standard algorithmus implementiert, aber in der dokumentation des Squarify Algorithmus steht explizit: 
\begin{quote}
    You probably also want to call root.sort to order the hierarchy before computing the layout. \cite{d3_treemap_code}
\end{quote}
Aus diesem Grund testen wir jetzt, ob das sortieren der Knoten nach Größe wirklich die besten auch für unsere Daten liefert und ob sich das auch auf andere Kriterien als das Seitenverhältnis auswirkt. 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/sorting/numberOfZeroAreaNodes_over_aimedRelativeMargin.png}
    \caption[Fehlende Knoten über relativen Abstand]{Anzahl der fehlenden Knoten in Abhängigkeit des relativen Abstands für Sortierung nach Größe und keiner Sortierung.}
    \label{fig:sorting_numberOfZeroAreaNodes_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/sorting/variationCoefficientOfAreaDifferences_over_aimedRelativeMargin.png}
    \caption[Flächenverhältnis über relativen Abstand]{Varianzkoeffizient des Flächenverhältnisses in Abhängigkeit des relativen Abstands für Sortierung nach Größe und keiner Sortierung.}
    \label{fig:sorting_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/sorting/averageRatio_over_aimedRelativeMargin.png}
    \caption[Seitenverhältnis über relativen Abstand]{Durchschnittliches Seitenverhältnis in Abhängigkeit des relativen Abstands für Sortierung nach Größe und keiner Sortierung.}
    \label{fig:sorting_averageRatio_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/sorting/spaceForLeafNodesRatio_over_aimedRelativeMargin.png}
    \caption[Platznutzung über relativen Abstand]{Platznutzung in Abhängigkeit des relativen Abstands für Sortierung nach Größe und keiner Sortierung. Werte näher bei 1 sind besser.}
    \label{fig:sorting_spaceForLeafNodesRatio_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/sorting/timeNeededInMs_over_aimedRelativeMargin.png}
    \caption[Zeitaufwand über relativen Abstand]{Zeitaufwand in Abhängigkeit des relativen Abstands für Sortierung nach Größe und keiner Sortierung.}
    \label{fig:sorting_timeNeededInMs_over_aimedRelativeMargin}
\end{figure}

In den wichtigsten Kriterien, Anzahl der fehlenden Knoten (siehe Abbildung \ref{fig:sorting_numberOfZeroAreaNodes_over_aimedRelativeMargin}), der Varianzkoeffizient des Flächenverhältnisses (siehe Abbildung \ref{fig:sorting_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin}) und das durchschnittliche Seitenverhältnis (siehe Abbildung \ref{fig:sorting_averageRatio_over_aimedRelativeMargin}) schneidet die Sortierung nach Größe durchweg besser ab, weshalb wir uns dafür entscheiden, die Knoten immer nach Größe zu sortieren.



\subsection{Platzierung der Knoten nach erstem Layoutschritt} \label{sec:ReihenfolgeKnotenErstesLayout}
Der Algorithmus ihrer Arbeit über Cascaded Treemap von Lu und Fogarty \cite{lu2008cascaded}, wird die Platzierung der Knoten im zweiten Layout-Schritt beibehalten. Im Grunde merken Sie sich in welcher Reihe an welcher Position jeder Knoten platziert ist und behalten diese Platzierung bei. D3.js stellt zu ihrem Squarify Algorithmus auch einen Resquarify Algithmus zu verfügung, der im Grunde diesen zweiten Layout-Schritt mit beibehaltung der Platzierung der Knoten durchführt \cite{d3_treemap_code}. D3.js begründet die existens dieser Strategie anders als Lu und Fogarty, nämlich damit, dass ein erneutes berechnen der Layouts mehr Zeit in anspruch nimmt. Dies macht Sinn weil wenn die Platzierung gleich bleibt, muss ich ja nur noch die Breite der Reihen und die Höhe der Knoten anpassen und nicht noch verschiedene Positionierungen testen und vergleichen.

Die Qualität dieser Strategie wird leider nicht separat evaluiert und hinterfragt. Wir stellen die Hypthese auf, dass durch diese Strategie aufgrund der verschiedenen Flächenänderungen schlechtere Seitenverhältnisse entstehen können, da die optimale Reihenfolge der Knoten sich ja durch die Flächenänderung ändern kann. Wir wollen also nachprüfen ob dieser Ansatz wirklich der beste ist, oder ob es nicht vielleicht bessere Strategien für den zweiten Layout-Schritt gibt.




Durch die updates der Größen der Knoten nach dem ersten Layoutschritt, kann es passieren, dass die Größenreihenfolge der Knoten sich ändert. Das als zum Beispiel Knoten A im ersten Layoutschritt größer ist als Knoten B, aber nach dem Update der Größen von Knoten A kleiner ist als Knoten B. Das kann dazu führen, dass im folgenden Layoutschritt, die Knoten in einer anderen Reihenfolge platziert werden als im ersten Layoutschritt. 
Das ist erstmal gut für die Seitenverhältnisse der Knoten (wenn wir davon ausgehen, dass die Reihenfolge nach größe sortiert optimla ist), allerdings kann es dazu führen, dass Knoten an ganz anderen Positionen platziert werden, als im ersten layoutschritt. Das  kann dazu führen, dass sich die Form ändert und das kann dazu führen, dass die Kinder anders platziert werden etc. wodurch sich die benötigten Abstände ändern, wodurch die vorausberechnete Fläche der Knoten nicht mehr passt und Knoten entweder verschwinden oder die Wert Fläche relation schlechter wird.
Das kann natürlich genauso passieren, wenn die Knoten in der selben Reihenfolge platziert werden, wie im ersten Layoutschritt. Es ist nicht klar erstichtlich, was hier am Ende des Tagen am besten ist. Deswegen implementieren wir 3 verschiedene Ansätze:
\begin{itemize}
    \item \textbf{Reihenfolge der Knoten nach Größe:} Die Knoten werden in der Reihenfolge ihrer neu berechneten Größe platziert. Wir vermuten, dass dadurch die besten Seitenverhältnisse entstehen, aber die schlechtesten Wert-Fläche-Relationen, weil sich die Seitenverhältnisse im vergleich zum ersten Layoutschritt stark ändern können und dadruch die vorausberechnete Fläche der Knoten nicht mehr passt.
    \item \textbf{Reihenfolge der Knoten beibehalten:} Die Knoten werden in der Reihenfolge platziert, in der sie im ersten Layoutschritt platziert wurden (absteigend sortiert). Wir vermuten, dass dadurch im Großen und Ganzen die plazierung der Knoten gleich bleibt, außer es gibt wirklich große Flächen updates. Wir vermuten, dass dies ein guter mitelweg zwischen dem ersten und dem jetzt folgenden ansatz ist.
    \item \textbf{Platzierung der Knoten beibehalten:} Nach dem ersten Layoutschritt wird sich genau die Aufteilung der Knoten in Reihen und die Platzierung der Knoten in den Reihen gemerkt. Wir vermuten, dass dadurch die Form der Knoten im großen und ganzen ähnlich zum ersten Schritt bleibt, wodurch die Wert-Fläche-Relationen besser bleibt, aber die Seitenverhältnisse schlechter werden könnten, die die optimale Reihenfolge der Knoten (aufgrund der neuen werte) nicht mehr berücksichtigt wird.
    \item 
\end{itemize} 
Es ist wie gesagt nicht ganz klar, was genau die auswirkungen sind. deswegen nur vermutungen, die wir jetzt testen werden.


\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/secondsorting3/numberOfZeroAreaNodes_over_aimedRelativeMargin.png}
    \caption[Fehlende Knoten über relativen Abstand]{Anzahl der fehlenden Knoten in Abhängigkeit des relativen Abstands für die drei verschiedenen Platzierungsstrategien.}
    \label{fig:secondsorting3_numberOfZeroAreaNodes_over_aimedRelativeMargin}
\end{figure}

In Abbildung \ref{fig:secondsorting3_numberOfZeroAreaNodes_over_aimedRelativeMargin} ist klar zu erkennen, dass die Strategie, die Knoten in der Platzierung beizubehalten, die meisten fehlenden Knoten produziert. Die anderen beiden Strategien schneiden deutlich besser ab. In Abbildung \ref{fig:secondsorting2_numberOfZeroAreaNodes_over_aimedRelativeMargin} ist genauer zu erkennen, dass die Strategie, die Knoten neu zu sortieren weniger fehlende Knoten produziert als die Strategie, die Reihenfolge der Knoten beizubehalten.

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/secondsorting2/numberOfZeroAreaNodes_over_aimedRelativeMargin.png}
    \caption[Fehlende Knoten über relativen Abstand]{Anzahl der fehlenden Knoten in Abhängigkeit des relativen Abstands für die zwei verschiedenen Platzierungsstrategien.}
    \label{fig:secondsorting2_numberOfZeroAreaNodes_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/secondsorting3/variationCoefficientOfAreaDifferences_over_aimedRelativeMargin.png}
    \caption[Flächenverhältnis über relativen Abstand]{Varianzkoeffizient des Flächenverhältnisses in Abhängigkeit des relativen Abstands für die drei verschiedenen Platzierungsstrategien.}
    \label{fig:secondsorting3_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin}
\end{figure}

Anders als erwartet, ist in Abbildung \ref{fig:secondsorting3_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin} zu erkennen, dass alle drei Strategien ähnliche Ergebnisse beim Varianzkoeffizienten des Flächenverhältnisses erzielen. 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/secondsorting3/averageRatio_over_aimedRelativeMargin.png}
    \caption[Seitenverhältnis über relativen Abstand]{Durchschnittliches Seitenverhältnis in Abhängigkeit des relativen Abstands für die drei verschiedenen Platzierungsstrategien.}
    \label{fig:secondsorting3_averageRatio_over_aimedRelativeMargin}
\end{figure}

wie erwartet, ist in Abbildung \ref{fig:secondsorting3_averageRatio_over_aimedRelativeMargin} zu erkennen, dass die Strategie, die Knotenplatzierungen bei zu behalten die schlechtesten Seitenverhältnisse produziert. Die anderen beiden Strategien schneiden deutlich besser ab, untereinander verglichen sind sie aber ähnlich.

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/secondsorting3/spaceForLeafNodesRatio_over_aimedRelativeMargin.png}
    \caption[Platznutzung über relativen Abstand]{Platznutzung in Abhängigkeit des relativen Abstands für die drei verschiedenen Platzierungsstrategien. Werte näher bei 1 sind besser.}
    \label{fig:secondsorting3_spaceForLeafNodesRatio_over_aimedRelativeMargin}
\end{figure}

In Abbildung \ref{fig:secondsorting3_spaceForLeafNodesRatio_over_aimedRelativeMargin} ist zu erkennen, dass alle drei Strategien ähnliche Ergebnisse bei der Platznutzung erzielen.


\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/secondsorting3/timeNeededInMs_over_aimedRelativeMargin.png}
    \caption[Zeitaufwand über relativen Abstand]{Zeitaufwand in Abhängigkeit des relativen Abstands für die drei verschiedenen Platzierungsstrategien.}
    \label{fig:secondsorting3_timeNeededInMs_over_aimedRelativeMargin}
\end{figure}

In Abbildung \ref{fig:secondsorting3_timeNeededInMs_over_aimedRelativeMargin} wird die Erwartung bestätigt, dass die Strategie, die Knotenplatzierungen beizubehalten am wenigsten Zeit benötigt. Da hier der Squarify Algorithmus nur einmal beim ersten Layoutschritt laufen muss. Die anderen beiden Strategien benötigen mehr Zeit, da hier der Squarify Algorithmus zweimal laufen muss. Die Reihenfolge beizubehalten benötigt minimal weniger Zeit (im Schnitt 0,2ms weniger), da hier nur einmal sortiert werden muss. Dies ist aber eine vernachlässigbare Zeitspanne.

\smallskip

Alles in allem konnten wir zeigen, dass der Ansatz von Lu und Fogarty \cite{lu2008cascaded}, die Knotenplatzierung beizubehalten, für unsere Daten und Kriterien nicht der beste ist. Der Ansatz der neuen Sortierung der Knoten nach Größe schneidet bis auf die Laufzeit in allen Kriterien deutlich besser als Lu und Fogartys Ansatz ab und ist entweder gleich oder etwas besser als der Ansatz, die Reihenfolge der Knoten beizubehalten. Wir entscheiden uns in dieser Arbeit dafür, die Knoten im zweiten Layoutschritt nach Größe zu sortieren, da wir hier die besten Seitenverhältnisse erzielen und die Anzahl der fehlenden Knoten im vergliech zu den anderen beiden Ansätzen gering bleibt.












\section{Mehrfache Berechnung} \label{sec:MehrfacheBerechnung}
Die Grundlegende Idee dieser Erweiterung ist es, das Layout mehrfach zu berechnen und dabei die Fläche der Knoten immer weiter anzupassen. 
Warum glauben wir, dass das gut ist. nach dem ersten update schritt ist die vorhersage der neuen fläche ja oft nicht so gut, und es kann sich noch einiges in der reihenfolge der Knoten ändern oder die Form der Knoten. dadurch ist nach dem zweiten Layout-Schritt die platzierung ja anders, dadurch stimmt die vorhergesagte Fläche der Knoten nicht mehr. dann könnte man nochmal her gehene und die fläche neu berechnen und nochmal den layoutschritt machen. Wir führen dafür einen neuen Größenanpassungsschritt ein. Dieser Schritt ist ähnlich zu dem relativen Größenanpassungsschritt (siehe Listing \ref{alg:RelativeGrößenanpassung}).


Wir stellen also die Hypthese auf, dass durch mehrfaches berechnen des layouts und das aktualisieren der flächen, alle unsere Kriterien, bis auf die laufzeit, besser werden. 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/multipleRuns/numberOfZeroAreaNodes_over_aimedRelativeMargin.png}
    \caption[Fehlende Knoten über relativen Abstand]{Anzahl der fehlenden Knoten in Abhängigkeit des relativen Abstands für 2, 4, 6, 8, 10 Layout-Schritte.}
    \label{fig:multipleRuns_numberOfZeroAreaNodes_over_aimedRelativeMargin}
\end{figure}

Es ist ganz klar in Abbildung \ref{fig:multipleRuns_numberOfZeroAreaNodes_over_aimedRelativeMargin} zu erkennen, zwei Layout-Schritte dauerhaft bessere Ergebnisse erzielen als mehrere Layout-Schritte. Auch alle anderen Kriterien sind entweder gleich oder schlechter. Die Laufzeit ist je nach Anzahl der Layout-Schritte um entsprechenden Faktor höher. Es gibt also keinen Grund, warum die mehrfache Berechnung des Layouts sinnvoll sein sollte. Wir vermuten dass das daran liegt, dass Knoten, die besonders viel Abstand hinzubekommen


\subsection{Iterative Abstandsvergrößerung} \label{sec:IterativeAbstandsvergrößerung}
Wir haben soeben gesehen, dass die mehrfache Berechnung des Layouts nicht sinnvoll ist, so wie sie implementiert ist. Wir vermuten, dass das daran liegt, 

Die Grundlegende Idee dieser Erweiterung ist es, das wenn das Layout mehrfach berechnet wird, es besser ist, den Abstand zwischen den Knoten iterativ von Schritt zu Schritt zu vergrößern, anstatt ihn von Anfang an auf den finalen Wert fest zu setzen. 
Warum gehen wir von dieser Annahme aus? 
Wenn man im ersten Schritt den Abstand auf den finalen Wert setzt, dann werden die Vorhersagen für die neue Fläche der Knoten im zweiten Schritt nicht exakt sein, das ist ja klar. Umso kleiner die Abstände sind, umso kleiner sind auch die Abweichungen dieser Vorhersagen. Dadurch sollten die Wer-Fläche-Relationen im zweiten Schritt besser sein. 
%zeige das anhand von daten
Die Annahme ist jetzt, dass wenn man jedes mal den Abstand um einen kleinen Wert vergrößert, dann sind in jedem Schritt die Vorhersagen für die Fläche der Knoten genauer, wodurch die Wert-Fläche-Relationen besser werden. Als Gegenargument könnte man bringen, dass dadurch, dass jedes mal die Abstände größer werden auch jedes mal das Optimale Layout (man kann das optimale layout nicht berechnen, aber wir gehen mal von einem theoreitschen optimalen Layout aus) auch in jedem layout schritt anders sein wird. Dadurch wird erst im letzen Layoutschritt das "wahre" optimale Layout angestrebt, wodurch die vorherigen schritte unnötig waren.

Wir überprufen jetzt die annahme und schauen, welche begründung richtiger ist/welcher effekt mehr gewicht hat. 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/incrementMargin/numberOfZeroAreaNodes_over_aimedRelativeMargin.png}
    \caption[Fehlende Knoten über relativen Abstand]{Anzahl der fehlenden Knoten in Abhängigkeit des relativen Abstands für 2 Layout-Schritte mit direkter Abstandsvergrößerung und 2 Layout-Schritte mit iterativer Abstandsvergrößerung.}
    \label{fig:incrementMargin_numberOfZeroAreaNodes_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/incrementMargin/variationCoefficientOfAreaDifferences_over_aimedRelativeMargin.png}
    \caption[Flächenverhältnis über relativen Abstand]{Varianzkoeffizient des Flächenverhältnisses in Abhängigkeit des relativen Abstands für 2 Layout-Schritte mit direkter Abstandsvergrößerung und 2 Layout-Schritte mit iterativer Abstandsvergrößerung.}
    \label{fig:incrementMargin_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin}
\end{figure}





\section{Optimierung von Ordnerketten} \label{sec:OrderKettenOptimierung}
Bei der Datenanalyse ist aufgefallen, dass es oft vorkommt, dass Ordner nur ein Kind haben bzw. dass es \textit{Ketten} von Ordnern gibt, die nur ein Ordner als Kind haben. Häufig sind diese Ketten auch nicht nur einen Ordner tief, was gar nicht so viel Abstand bzw platz verbrauchen würde, doch es gibt auch einige ketten die 4 ordner tief sind, was dann schon wieder (mehr) als viel mal so viel platz für abstände auf braucht durch die Verschachtelung.
obwohl im Grunde diese Schachtelung keine wirklichen Strukturinformationen enthält. Viele IDEs wie zum Beispiel auch IntelliJ IDEA oder VSCode verbergen diese Ordnerstruktur zum beispiel auch automatisch, in dem solche ordner ketten nur als ein Ordner mit entsprechendem zusammen gefügten namen (in dem fall \textit{main/kotlin/de/maibornwolff/codecharta/analysers/analyserinterface}) angezeigt wird.
Diese Optimierung kann auch für unsere Visualisierung übernommen werden, indem solche Ordnerketten zu einem Ordner zusammengefasst werden. 

Da dies nicht nur eine Algorithmische Änderung ist, sondern auch einen direkten Einfluss auf die Visualisierung hat, werden wir nicht nur die Auswirkungen auf die messbaren Kriterien untersuchen, sondern auch kurz diskutieren, welche Auswirkungen dies auf die generellen Kriterien von Visualiserungen hat, wie wir sie in Abschnitt \ref{sec:MethodikAuswertung} definiert haben.
Es ergeben sich folgende Auswirkungen auf die Ziele der Visualisierung:
\begin{itemize}
    \item \textbf{Informationsgehalt und effiziente Nutzung des Platzes:} Wird verbessert, da weniger Platz für Abstände zwischen den Ordnern benötigt wird und weniger Knoten im Layout sind. 
    \item \textbf{Niedrige visuelle Komplexität und Abstraktion:} Wird verbessert, da Codebasen mit tiefen Ordnerstrukturen einfacher dargestellt werden können.  
    \item \textbf{Skalierbarkeit:}  Wird verbessert, da weniger Knoten im Layout sind und große Codebasen mit tiefen Ordnerstrukturen einfacher dargestellt werden können.
    \item \textbf{Visuelle Metaphern:} Dies hat keine Auswirkungen auf die visuelle Metapher der Stadt.
\end{itemize}

Wir würden sagen, dass die Vorteile dieser Änderung die Nachteile stark überwiegen.

Im Schnitt werden ca. 175 Ordner pro Codebasis gefaltet (12 Median). bei ca. 958 Knoten (Median 242) sind das ca. 18\% (bzw. 5\% Median) der Knoten die durch die Faltung verschwinden. Also schon einige. 

Folgend kommen die DAten:

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/folding/numberOfZeroAreaNodes_over_aimedRelativeMargin.png}
    \caption[Fehlende Knoten über relativen Abstand]{Anzahl der fehlenden Knoten in Abhängigkeit des relativen Abstands für gefaltete und ungefaltete Ordnerketten.}
    \label{fig:foldingChains_numberOfZeroAreaNodes_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/folding/variationCoefficientOfAreaDifferences_over_aimedRelativeMargin.png}
    \caption[Flächenverhältnis über relativen Abstand]{Varianzkoeffizient des Flächenverhältnisses in Abhängigkeit des relativen Abstands für gefaltete und ungefaltete Ordnerketten.}
    \label{fig:foldingChains_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/folding/averageRatio_over_aimedRelativeMargin.png}
    \caption[Seitenverhältnis über relativen Abstand]{Durchschnittliches Seitenverhältnis in Abhängigkeit des relativen Abstands für gefaltete und ungefaltete Ordnerketten.}
    \label{fig:foldingChains_averageRatio_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/folding/spaceForLeafNodesRatio_over_aimedRelativeMargin.png}
    \caption[Platznutzung über relativen Abstand]{Platznutzung in Abhängigkeit des relativen Abstands für gefaltete und ungefaltete Ordnerketten. Werte näher bei 1 sind besser.}
    \label{fig:foldingChains_spaceForLeafNodesRatio_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/folding/timeNeededInMs_over_aimedRelativeMargin.png}
    \caption[Zeitaufwand über relativen Abstand]{Zeitaufwand in Abhängigkeit des relativen Abstands für gefaltete und ungefaltete Ordnerketten.}
    \label{fig:foldingChains_timeNeededInMs_over_aimedRelativeMargin}
\end{figure}




die Daten Zeigen in allen Aspekten eine Verbesserung durch das Falten von Ordnerketten. Wir sprechen uns also dafür aus, dass Ordnerketten immer gefaltet werden sollten, von den messbaren Kriterien her sehen wir keine Gründe, warum das nicht gemacht werden sollte. das einzige Ziel was dagegen spricht ist die Korrelation mit dem Code, die dadurch verschlechtert wird. Alles in allem sprechen wir uns also dafür aus und werden dies fix für unseren Algorithmus implementieren.









\section{Abstände zwischen Geschwister Knoten} \label{sec:AbständeGeschwister}
Abstände zwischen Geschwister Knoten sind eine wichtige Entscheidung. Im Ursprünglichen Squarify Algorithmus Paper \cite{bruls2000squarified} wird der Nested Ansatz mit Abständen vorgestellt, jedoch ohne Abstände zwischen Geschwistern. Viele Tools, verwenden jedoch Abstände auch zwischen Geschwisterknoten \cite{codeCity1, code_charta_webdemo, sereene_website}.
Was sind die Vor- und Nachteile von Abständen zwischen Geschwistern?
\begin{itemize}
    \item \textbf{Informationsgehalt und effiziente Nutzung des Platzes:} Wird verschlechtert, da jeder Knoten mit geschwistern mehr Platz benötigt. 
    \item \textbf{Niedrige visuelle Komplexität und hohe Verständlichkeit:} Wird verbessert, da die Knoten besser voneinander getrennt werden und somit die Struktur besser erkennbar ist. Allerdings muss das nicht der Fall sein, man könnte zum Beispiel die Kanten der Knoten mit einer anderen Farbe einfärben, um die Knoten besser voneinander zu trennen.
    \item \textbf{Skalierbarkeit:} Eigentlich wenig auswirkung auf skalierbarkeit, weder die laufzeit ändert sich signifikant (in unsrem algorithmus werden die abstände erzeugt, indem am ende alle knoten um den abstand verkleinert werden - also im grunde kommt laufzeit von N hinzu, wo N die Anzahl der Knoten ist, das ist aber nicht ausschlaggebend). Also wenn wird die Skalierbarkeit minimal schlechter.
    \item \textbf{Korrelation mit dem Code:} Keine auswirkung
    \item \textbf{Stabilität gegenüber Änderungen:} Auch nicht wirklich
\end{itemize}

Die Anpassung, die Knoten mit Kanten zu versehen, natürlich stark abhängig von der gewählten Farbgebung (auch Schattierung, Struktur oder Transparenz). Wie in der Problemstellung definiert (siehe Abschnitt \ref{sec:Problemstellung}) sollte das Layout so gestaltet sein, dass es möglich ist, weitere Metriken durch Farbgebung zu visualisieren. Die Kanten widersprechen dieser Einschränkung nur in Aussahme fällen. Die meisten Visualisierungen in der Praxis verwenden einfache Einfärbungen \cite{codeCity1, code_charta_webdemo, sereene_website}, was bedeutet, dass man einfach Schwarz oder eine Komplementärfarbe als Kantenfarbe nutzen kann, ohne dass es zu Konflikten mit der Farbgebung kommt. Die einzige Ausnahme, die mir bekannt ist, ist wäre die Nutzung von Linien, Schachbrettmuster oder ähnlichen Strukturen, durch die eine Kante nicht mehr als solche erkennbar wäre. Dies könnte zum Beispiel bei dem Ansatz aus dem paper \textit{E-Quality} von Ural Erdemir et al. \cite{eQuality} der Fall sein, da hier zum Beispiel ein Gittermuster verwendet wird, um einen Mangel an Kohäsion zu visualisieren, eine Kante, selbst in anderer Farbe, würde hier eher stören oder in dem Muster untergehen. 

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/siblingMargin/numberOfZeroAreaNodes_over_aimedRelativeMargin.png}
    \caption[Fehlende Knoten über relativen Abstand]{Anzahl der fehlenden Knoten in Abhängigkeit des relativen Abstands für Abstände zwischen Geschwistern und keine Abstände zwischen Geschwistern.}
    \label{fig:margin_numberOfZeroAreaNodes_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/siblingMargin/variationCoefficientOfAreaDifferences_over_aimedRelativeMargin.png}
    \caption[Flächenverhältnis über relativen Abstand]{Varianzkoeffizient des Flächenverhältnisses in Abhängigkeit des relativen Abstands für Abstände zwischen Geschwistern und keine Abstände zwischen Geschwistern.}
    \label{fig:margin_variationCoefficientOfAreaDifferences_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/siblingMargin/averageRatio_over_aimedRelativeMargin.png}
    \caption[Seitenverhältnis über relativen Abstand]{Durchschnittliches Seitenverhältnis in Abhängigkeit des relativen Abstands für Abstände zwischen Geschwistern und keine Abstände zwischen Geschwistern.}
    \label{fig:margin_averageRatio_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/siblingMargin/spaceForLeafNodesRatio_over_aimedRelativeMargin.png}
    \caption[Platznutzung über relativen Abstand]{Platznutzung in Abhängigkeit des relativen Abstands für Abstände zwischen Geschwistern und keine Abstände zwischen Geschwistern. Werte näher bei 1 sind besser.}
    \label{fig:margin_spaceForLeafNodesRatio_over_aimedRelativeMargin}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{images/improve/siblingMargin/timeNeededInMs_over_aimedRelativeMargin.png}
    \caption[Zeitaufwand über relativen Abstand]{Zeitaufwand in Abhängigkeit des relativen Abstands für Abstände zwischen Geschwistern und keine Abstände zwischen Geschwistern.}
    \label{fig:margin_timeNeededInMs_over_aimedRelativeMargin}
\end{figure}








\section{Beschriftung der Knoten} \label{sec:BeschriftungKnoten}
Die Beschriftung der Knoten ist ein wichtiger Aspekt der Visualisierung, da sie es dem Nutzer ermöglicht, die Knoten zu identifizieren und zu verstehen, welche Informationen sie repräsentieren. Es gibt verschiedene Ansätze, um die Beschriftung der Knoten zu gestalten. Jeder Ansatz hat seine Vor- und Nachteile. Ein häufig verwendeter Ansatz ist wie zum Beispiel auch beim in Abschmijt VerwandteArbeiten \ref{sec:VerwandteArbeiten} vorgestellten Cascaded Treemap Ansatz \cite{lu2008cascaded}.
Ordner erhalten eine Zusätzliche horizontale Fläche, entweder unten oder oben, die den Namen des Ordners enthält. 
Dies nimmt allerdings viel Platz ein und erzeugt die selben schwierigkeiten für den Algorithmus, wie die abstände zwischen den Knoten. Außerdem kann es passieren, dass die Beschriftung zu lang für die breite der Knotens ist, wodurch die Beschriftung entweder abgeschnitten wird oder über den Knoten hinauswächst.
Hao Lü and James Fogarty schlagen vor dynamisch zu entscheiden, ob die Beschriftung der Knoten angezeigt werden soll oder nicht \cite{lu2008cascaded}. Wenn das Hinzufügen der Beschriftung den Knoten dazu führen würde, dass gar kein Platz mehr für den Knoten selbst übrig bleibt, wird die Beschriftung nicht angezeigt. Der Vorteil dieses Ansatzes ist, dass sicher gegangen wird, dass durch die Beschriftung kein Knoten verschwindet. Der Nachteil ist, dass selbst wenn die Änderung der Knotenfläche zum Knotenwert durch das hinzufügen der Beschriftung riesig wird (wenn die Fläche also super klein wird), die Beschriftung trotzdem hinzugefügt wird. 

Offensichlich wird das Hinzufügen von Beschriftungen alle unsere Kriterien verschlechtern.


%daten ja vis nein

%\cite{largeHierarchicalRepositories}
% \begin{figure}[h]
%     \centering
%     \includegraphics[width=0.8\textwidth]{images/improve/beschriftungAuf.png}
%     \caption{Beispiel Platzierung der Beschriftung auf der Treemap \cite{largeHierarchicalRepositories}.}
%     \label{fig:beschriftungAuf}
% \end{figure}


Wir schlagen einen statischen ansatz vor, bei dem nur die Beschriftungen der Top N order angezeigt werden. 
Die Wahl von N ist nun die Stellschraube für die nicht für jede Codebasis gleich ist. Es ist Stellschraube um zwischen Informationsgehalt und visuelle Komplexität zu balancieren.
\begin{itemize}
    \item Je höher N ist, desto mehr Informationen (also Ordnernamen) werden angezeigt, was den Informationsgehalt erhöht.
\item Je niedriger N ist, desto weniger Informationen werden angezeigt, was die visuelle Komplexität reduziert, viele Beschriftungen können schnell ablenken und die Visualisierung unübersichtlich machen.
\item Außerdem wird die Korrelation mit dem Code verbessert, da ein übertragen von Vsiaulisierung zur codebasis einfacher wird mit höherem N.
%Beweis, dass große Codebasen auch Tiefer sind als kleine und nicht zwingen mehr kinder haben.
\item Skalierbarkeit hängt nicht ab von N, da N ja nicht mitwächst, sondern statisch ist, ob noch mehr order und noch mehr tiefe hinzu kommt, macht keinen wirklichen unterschied.
\item STabi gegen änderungen wenn überhaupt sinkt es, weil änderung vom namen zb. auf einmal sichtbar wird (das ist natürlich gewollt, trotzdem in unserer messung hier erstmal negativ- wenn auch vernachlässigbar) - eventuell werden dadurch in zwei versionen die selben Knoten, die jetzt anders heißen, nicht mehr so einfach als gleich identifiziert, obwohl diese die selbe form haben und ohne die beschriftung vielleicht anhand der form als gleich identifiziert werden würden - zugegeben sehr theoretisch und wahrscheinlich nicht relevant aber trotzdem erwähnenswert.
\end{itemize}
Mit diesem Ansatz hat man natülich immernoch die gleichen Probleme wie eingangs beschrieben, allerdings sind diese abhängig von dem wert N und außerdem werden diese minimiert, da wirklich nur die Top Knoten beschriftetet werden, wodurch die Fläche der unteren Knoten trotzdem besser vorhergesagt werden kann und sich dieser fehler nicht nach oben propagiert.

%wie genau wirkt sich die wahl von N aus?




\section{Fazit}
Wir haben in diesem Kapitel verschiedene Erweiterungen und Anpassungen des Squarify Algorithmus für unsere Anforderungen vorgestellt und evaluiert. Dabei haben wir verschiedene bestehende Ansätze aus der Literatur getestet. 
Der Cascaded Algorithmus von Lu und Fogarty \cite{lu2008cascaded} hat dabei einige Ideen gegeben. Ihr Ansatz am besten mit dem Absoluten Größenanpassung mit Skalierung und festen Knotenplatzierungen zu vergleichen.
Dadurch dass die Autoren ihren Ansatz nicht speziell für Softwarequalitätmetriken optimiert haben, konnten wir für einige Ideen zeigen, dass sie auch für unsere Anforderungen gut funktionieren und für andere, dass unsere Ideen besser funktionieren. Der einzige Ansatz der glaube ich nicht weiter verfolgt werden muss ist die Mehrfache Berechnung des Layouts, ich hatte nicht dass gefühlt, dass man hier noch anpassungen an dieser Idee vornehmen könnte, die zu besseren Ergebnissen führen.
Außerdem können die verwendeten Ansätze selbst noch weiter verbessert werden. Die hier verwendeten Ansätze sind zum großteil komplett neu und in noch keiner anderen Arbeit vorgestellt. Diese Ansätze sind V1 versionen und können sicherlich noch weiter verbessert werden. 

Das Treemap problem ist durch unseren Ansatz stark verbessert worden, allerdings ist es immernoch ein NP-Hartes Problem, weshalb es keine perfekte Lösung gibt. Es gibt sicherlich noch unzählige Ideen, um unseren Ansatz zu verbessern, wie zum Beispiel: Anstatt feste Abstände. Dynamische Abstände die sich an Tiefe oder Anzahl der Geschwister orient. Oder andere Größenanpassungen, die auf anderen Heuristiken basieren. Oder andere Sortier- und Platzierungsstrategien. Oder andere Ansätze um die Knoten zu beschriften. Oder oder oder... 


Alles in allem Empfehlen wir folgende Einstellungen für den Algorithmus:
\begin{itemize}
    \item \textbf{Abstände zwischen Geschwistern:} Manuelle Wahl zwischen 0,5\% und 3\% relativem Abstand je nach Software und Ziel der Visualisierung (siehe Abschnitt \ref{sec:AbständeGeschwister})
    \item \textbf{Angestrebtes Seitenverhältnis:} 1,05 (siehe Abschnitt \ref{sec:Seitenverhältnis})
    \item \textbf{Sortierung der Knoten:} Absteigend nach Größe (siehe Abschnitt \ref{sec:ReihenfolgeKnotenErstesLayout})
    \item \textbf{Platzierung der Knoten nach erstem Layoutschritt:} Neue Sortierung absteigend nach Größe (siehe Abschnitt \ref{sec:ReihenfolgeKnotenErstesLayout})
    \item \textbf{Mehrfache Berechnung:} Nein (siehe Abschnitt \ref{sec:MehrfacheBerechnung})
    \item \textbf{Optimierung von Ordnerketten:} Ja (siehe Abschnitt \ref{sec:OrderKettenOptimierung})
    \item \textbf{Beschriftung der Knoten:} Manuelle Wahl zwischen x und y (siehe Abschnitt \ref{sec:BeschriftungKnoten})
\end{itemize}

Wir werden exakt diese Einstellungen verwenden, um die Visualisierungen für die Umfrage im nächsten Kapitel zu erstellen.